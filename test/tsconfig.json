{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": [
      "jest",
      "node"
    ],
    "rootDir": ".." // Point to project root to include both source/ and test/
  },
  "include": [
    "**/*.ts",
    "../source/**/*.ts"
  ],
  "exclude": [
    "../node_modules",
    "../distribution"
  ]
}